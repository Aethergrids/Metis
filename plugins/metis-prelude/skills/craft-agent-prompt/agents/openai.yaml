interface:
  display_name: "Craft Agent Prompt"
  short_description: "Create lean, testable agent prompt contracts"
  default_prompt: "Use $metis-prelude:craft-agent-prompt to audit and simplify this agent prompt against GPT-5.6 guidance."
