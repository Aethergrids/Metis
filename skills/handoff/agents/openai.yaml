interface:
  display_name: "Handoff"
  short_description: "Transfer work into a fresh, compact context"
  default_prompt: "Use $handoff to prepare a compact transfer for a fresh task focused on the next objective."

policy:
  allow_implicit_invocation: false
